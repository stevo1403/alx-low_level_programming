#include "main.h"

/**
 * add - Adds two integers and returns the result
 * @n1: the first operand to the add operator
 * @n2: the seconds operand to the add operator
 *
 * Return: the addition of `n1` and `n2`
 */
int add(int n1, int n2)
{
	int r;

	r = n1 + n2;
	return (r);
}
